name = "my-agent"
pages_build_output_dir = "public"

# 会話専用（最小構成）。/api/chat は Gemini API のみを使うため D1/KV/AI バインドは不要。
# シークレット（wrangler pages secret put で設定）:
#   GEMINI_API_KEY … AI Studio の Gemini APIキー
